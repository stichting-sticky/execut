---
import { Image } from 'astro:assets'
import { getCollection } from 'astro:content'

import { Icon } from 'astro-icon/components'

import hero from '~/assets/hero.jpg'

import Layout from '~/layouts/base.astro'

import Footer from '~/components/footer.astro'
import Header from '~/components/header.astro'
import Hero from '~/components/hero.astro'
import Section from '~/components/section.astro'

const [entry] = await getCollection('editions').then((entries) =>
  entries.slice(-1),
)

const { id, data } = entry

const { date } = data
---

<Layout edition={id}>
  <nav
    class="sticky top-0 z-50 flex w-full flex-wrap items-center justify-center bg-gradient-to-r from-orange/80 via-red/80 to-light-red/80 drop-shadow-md backdrop-blur sm:static sm:justify-between sm:bg-none sm:px-4 sm:drop-shadow-none sm:backdrop-blur-none lg:my-12 lg:flex-row lg:flex-nowrap lg:px-8">
    <a
      class="mt-10 hidden items-center sm:flex sm:justify-between sm:pl-8 lg:mt-0 lg:grow lg:basis-0 lg:justify-start"
      href="/">
      <Icon class="h-12 w-12" name="icon-terminal" />
    </a>

    <a
      class="order-first flex flex-auto basis-full justify-center overflow-x-auto py-4 text-center font-mono text-sm leading-relaxed text-white sm:border-b sm:border-red/20 sm:py-4 sm:text-red lg:order-none lg:mx-0 lg:basis-auto lg:border-0 lg:py-0"
      href="/">
      {date.getFullYear()}
      <span class="mx-2 text-white/60 sm:text-red/40">{'|>'}</span>
      TBA
    </a>

    <span></span>
  </nav>

  <Header>
    <h1
      class="my-6 font-mono text-5xl font-bold text-red sm:mt-24 sm:text-6xl sm:leading-snug">
      A conference for and by IT students.
    </h1>

    <h2
      class="mb-8 mt-2 text-2xl leading-snug text-red/80 sm:text-3xl sm:leading-normal">
      exec(ut) will not be back! The 2025 committee is hard at work to realize
      the next edition.
    </h2>
  </Header>

  <Section id="about">
    <p class="mt-4 sm:mt-6">
      exec(ut) is an event organised by Stichting Sticky. It is a conference for
      and by students, offering a glimpse at the wondrous world of IT beyond the
      university. We strive to provide a diverse programme and show how exciting
      conferences can be.
    </p>
  </Section>

  <Hero class="bg-subtle-highlight">
    <Image class="shadow-2xl" src={hero} alt="exec(ut) 2024 atmosphere" />
  </Hero>

  <Section id="contact">
    <p class="mt-4 sm:mt-6">
      Without the generous support of our partners' exec(ut) will impossible to
      organize. If you are intrested in working with us to make the next edition
      even better, please contact us at
      <a
        class="font-mono text-green underline transition-colors hover:text-green/80"
        href="mainto:partners@execut.nl">partners@execut.nl</a
      >.
    </p>

    <p class="mt-4 sm:mt-6">
      Want to speak at exec(ut)? Please send us your talk proposal to
      <a
        class="font-mono text-green underline transition-colors hover:text-green/80"
        href="mainto:speakers@execut.nl">speakers@execut.nl</a
      > and we will have a look.
    </p>
  </Section>

  <Footer />
</Layout>
