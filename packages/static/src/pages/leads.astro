---
import { getCollection } from 'astro:content'

import Layout from '~/layouts/base.astro'

import Scanner from '~/components/Leads'

const [entry] = await getCollection('editions')
  .then((entries) => entries.slice(-1))

const { id } = entry
---

<Layout edition={id}>
  <Scanner client:only />
</Layout>
